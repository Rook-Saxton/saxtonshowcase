# Plot Twist, a challenge randomizer for life-sim runs

Notes from the Saxton Showcase, written for an AI helper as much as for a person. Hand this file to your agent along with `plot-twist.html`, the game itself, and it gets what we learned without having to rebuild it to find out.

- Built: 2026-09-23
- Tags: tool, life-sim, randomizer, challenge, checklist, touch, keyboard, web audio
- The game: one self contained HTML file. Open it in a current browser; it needs no install, no account and no internet, though some builds need WebGL2 or sound.

## What it is

A single-file browser tool for people who play life-simulation games. Deal (button or Space) spins seven cards and flips them over: a starting budget band, a household setup, a house rule, a career direction as a broad job family, a personality to play, a goal and a twist that lands on a set day. Any card can be locked while the rest re-roll. A difficulty dial (cosy, spicy, chaotic) weights which entries can appear, and every entry carries its own tag. Play this run turns the run into a checklist with a day counter, and the twist stays sealed until its day. Runs save to a history, share as a short code (base-36 seed and dial, then the list version, then a group for each locked card that came from an earlier deal) that deals the exact same run, and download as a 1080x1350 PNG card. All 214 entries (20 to 25 per category, 66 twists) are our own generic writing and can be edited on the My lists screen: add, remove, retag, reset one list or all. History and lists export to and import from a .json file. Sound is synthesized, starts after the first input and has a mute button plus the M key. ?preview=1 deals run after run with the spin and no chrome.

## What we learned building it

Locking and share codes pull against each other. A locked card came from an earlier seed, so one seed cannot reproduce a run once anything is locked. The fix was to give every card its own sub-seed (the run seed mixed with the card's position) and have each card remember the seed and dial it came from. The code is then the run's seed plus one short group per locked-in source, and a plain run's code stays short (S1IY5ZT3-EAXWZ). The list version is a hash of every entry, its tag and its order, so any edit changes it. Undoing an edit or resetting the lists brings the old version back, so default codes work for anyone on default lists. Keeping the logic pure also meant node could chain 2400 random re-rolls and check that the final code still deals the exact run.

## The gotcha

The slot reels stopped a few pixels off their final row, and the error grew with each card (2 px on the first, 8 px on the last in the preview). The first version measured the reel row height while the card back was still display:none and fell back to a guessed 36 px. Measuring after the flip used offsetHeight, which rounds 36.8 px down to 36 and drifts 0.8 px per row over a 12 to 24 row strip. getBoundingClientRect was worse, because it includes the 3D flip transform running on the card. The height from getComputedStyle is unrounded and ignores transforms, and fixed it. A browser check now compares every stopped reel with its final row at desktop, phone and preview sizes (within 0.3 px). A smaller one: the first list-version hash used only the first letter of each tag, and cosy and chaotic both start with c, so retagging between them did not change the version. A node test caught it.

## How we checked it

node test_logic.js: 127 passed, 0 failed. Covered: every category has 20+ entries with 5+ per tag, and there are 66 twists. The same seed and lists deal identical runs (500 seeds). Every entry is reachable over 3000 deals. Locked cards stay unchanged through 2400 chained re-rolls, and unlocked cards match a fresh deal. Codes after a re-roll chain still deal the exact run (400). All three dials deal 2000 full runs each, within their tags. Lists stripped to a single tag, or cut to one entry, still deal full runs, with an off-dial flag where needed. Twist days fall in each dial's range. 1000 random codes round-trip, including lower case and spaces. 12 malformed codes are rejected. Adding, removing, retagging, reordering or rewording an entry changes the version, and an old code with edited lists returns a warning instead of a run. Import sanitising works. node check_ui.js (Playwright, system Chrome): 66 passed, 0 failed. At 1280x720 by keyboard and mouse: cards and action tile fit, Space deals and spins, Space mid-spin stops the reels, reels stop within 1 px of their final row, locks (key 1 and a click) survive 5 re-rolls, D turns the dial, cosy and chaotic deals stay within their tags, the share dialog shows the code, the PNG downloads (about 200 KB), Escape closes the dialog with defaultPrevented false, pasting a code (lower case) deals the same run, a code from a locked re-roll deals that exact run, a bad code shows a message, Play saves to history and opens the checklist, the twist is sealed on day 1 and opens on its day, ticks and day survive a reload, all seven ticked shows run complete, adding and removing list entries changes the version, the old code then warns and deals nothing, 'deal it from the default lists' deals the original, reset all restores the default version, export, delete and import round-trip the history, and M and the mute button work. At 390x844 with touch: no horizontal overflow on any screen, cards two across, tap to deal, lock and re-roll, tick and advance days, reels aligned. With localStorage throwing: deal and play still work and a toast says saving is unavailable. Reduced motion: deals instantly with no spin. ?preview=1 at 600x300: two frames 3 s apart differ, no header, bar, buttons or tile visible, the grid fits the frame, reels aligned, 7 deals in 30 s, zero localStorage writes, no AudioContext created. Zero page or console errors in every run. Screenshots reviewed at both sizes: deal, mid-spin, locked cards, share, code warning, checklist, lists, history, the PNG card, and preview at 600x300 and 350x175. After review I fixed the PNG footer overlapping the last row, reels landing off-row, empty space under the desktop grid, a hard edge behind the sticky bar, and clipped chips at 350x175.

## About these notes

Copied word for word from the build's own record. Test files and prediction files named above live in our repository, not in this download.

Copyright (c) 2026 Amelia Saxton. MIT License; see LICENSE, or the notice at the top of the game file.
