# Promptling, bite-size lessons for using AI well

Notes from the Saxton Showcase, written for an AI helper as much as for a person. Hand this file to your agent along with `promptling.html`, the game itself, and it gets what we learned without having to rebuild it to find out.

- Built: 2026-09-23
- Tags: learning, game, AI literacy, daily habit, touch, keyboard, web audio
- The game: one self contained HTML file. Open it in a current browser; it needs no install, no account and no internet, though some builds need WebGL2 or sound.

## What it is

A daily-habit learning app in one offline HTML file. A winding path of 31 nodes across six units (what an AI chat actually is, asking well, checking what it says, staying safe, getting real things done, knowing the limits): 25 lessons plus a review node closing each unit. Lessons are 6 to 8 quick exercises of seven kinds: multiple choice, which prompt is better, spot the made-up part, build the prompt (pick the ingredients a prompt has or lacks), safe or not safe (tap or swipe), put the steps in order, and fix it (choose the best follow-up after a disappointing reply), plus a pick-all-that-apply variant. Right answers get a bright bar, a chime and a one or two sentence reason; wrong answers cost a heart, show the reason and the answer, and return at the end of the lesson. Five hearts refill one per thirty minutes of real time, or all at once through a practice round of questions already seen. XP, a daily goal ring, a day streak counted in calendar days in the player's own time zone, a finish screen with confetti, and a profile with total XP, streak, best streak, lessons done and a confirmed reset. Pip, the mascot, is a teal speech bubble with a star on its head, drawn as inline SVG. No real AI is called; the whole curriculum (169 exercises) is one data block between LESSONS-BEGIN and LESSONS-END markers. ?preview=1 runs a silent 2:1 attract loop with a visible fake finger.

## What we learned building it

Keeping the curriculum as a pure data block made the accuracy rules testable rather than a promise: a ban on any digit in lesson text is a cheap, blunt proxy for no statistics, dates or prices, and it plus a list of product, company and month names runs on every check, with planted defects to prove the scanner bites. Writing the spot-the-made-up-part exercises around a snippet the learner pasted (a flyer, an invitation, a letter) meant the correct answer is checkable inside the exercise itself, so the lessons never had to assert a single fact about the outside world to teach fact-checking. On the layout side, CSS media queries answer to the real viewport, not to a scaled stage: the preview is a 640 by 320 stage scaled into a 600 px iframe, so the phone rules (stacked feedback bar, full-width buttons) fired inside it and the bar covered the answer it was praising, until preview-specific overrides were added.

## The gotcha

The path never scrolled to the player's current lesson, and every automated check still passed, because none of them looked. Only the preview screenshot showed it: the demo tapped a node while the view sat on unit one. The scroll used the node's offsetTop, but each node sits in a position:relative row, so offsetTop was measured from the row (about 12 px) rather than from the path. It now uses the difference of the two bounding rectangles, divided by the preview's scale factor so it also works on the scaled stage. A smaller one in the test itself: after Continue the feedback bar keeps its 'good' class and only loses 'on', so the first preview check counted the home screen as showing the right-answer bar; the check now requires both classes.

## How we checked it

node test_content.js: 103 passed, 0 failed. It counts 6 units, 25 lessons, 6 review nodes and 169 exercises (mc 58, tiles 25, pick 23, fix 18, order 16, safe 16, spot 7, need 6); every exercise has exactly one correct answer, set or order, an explanation, no empty strings and a unique id; all 169 accept their own answer and reject a wrong one; no digits, percent or currency signs, month names or product and company names in lesson text; streak, clock-change, heart refill, XP and unlock rules. 9 of 9 planted defects caught. node test_ui.js all (Playwright, system Chrome): 58 of 58 passed. Desktop 1280x720, 34 checks: welcome shows first, no AudioContext until the first key press, Enter starts lesson one, the whole lesson finished by keyboard with one deliberate miss that came back at the end (8 answers, 4 hearts, 10 XP, streak 1), Escape never default-prevented, M mutes and unmutes, a reload kept XP, streak and the finished lesson, five wrong answers ended lesson two on the out-of-hearts screen, the practice round refilled hearts to 5, a clock moved on 31 and 95 minutes showed 1 and 3 hearts, one day later a lesson took the streak to 2, after a skipped day the streak showed 0 and the next lesson restarted it at 1 with best kept at 2, the goal ring showed today's XP, and the profile reset cleared everything after a confirm. Phone 390x844 with touch, 12 checks: a whole lesson by taps (7 answers, 15 XP), every option and the Check button at least 44 px tall, no horizontal overflow, Continue always on screen, a mouse-drag swipe answered a safe-or-not card, losing all hearts by taps showed the practice offer, the page never scrolled. localStorage that throws, 3 checks: a whole lesson still plays and the page knows it cannot save. Preview at 600x300, 9 checks: preview class on html and body, no top bar, mute buttons or welcome card, frames 3 s apart differ, over 30 s it showed the path, a lesson, the right-answer bar and the finish, drew confetti, looped back to the path at least once, created no AudioContext and wrote nothing to localStorage. Zero page or console errors in every run. Screenshots reviewed at both sizes (welcome, path, node sheet, mid-lesson, finish, out of hearts, profile, every exercise type before and after checking) and preview at 600x300 and 350x175; fixes made from them: START tag overlapping the unit banner, sad mascot brows reading as angry, confetti carrying over onto the path, the brand icon hidden on phones, the path not scrolling to the current node, and the preview feedback bar covering the answer. An independent fact-check of all 169 exercises found 0 blockers and 11 fixes (plus 8 minor notes); all 11 fixes and all 8 notes were applied, after which node test_content.js passed 103 of 103 and node test_ui.js all passed 58 of 58, and all 18 pick and spot exercises with no q field were confirmed to show a default instruction. The session that added it to the showcase re-extracted the lessons after the fixes and confirmed all 11 reviewed exercises changed to the reviewer's wording, and reran test_content.js: 103 passed, 0 failed.

## About these notes

Copied word for word from the build's own record. Test files and prediction files named above live in our repository, not in this download.

Copyright (c) 2026 Amelia Saxton. MIT License; see LICENSE, or the notice at the top of the game file.
